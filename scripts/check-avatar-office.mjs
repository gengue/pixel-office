// Run with Playwright installed; PLAYWRIGHT_MODULE can point to an existing installation.
import assert from 'node:assert/strict'
const { chromium } = await import(process.env.PLAYWRIGHT_MODULE || 'playwright')
const browser = await chromium.launch({executablePath:'/usr/bin/chromium',args:['--no-sandbox','--use-fake-device-for-media-stream','--use-fake-ui-for-media-stream']})
const errors=[]
try {
 const ctx=await browser.newContext({permissions:['camera','microphone'],viewport:{width:1400,height:1000},recordVideo:{dir:'/tmp/human-office-video',size:{width:1400,height:1000}}})
 const a=await ctx.newPage(),b=await ctx.newPage()
 for(const p of [a,b])p.on('pageerror',e=>errors.push(e.message))
 const base=process.env.OFFICE_URL || 'https://workbox.bengal-balance.ts.net:8444/'
 await a.goto(base); await a.emulateMedia({reducedMotion:'reduce'})
 await a.waitForTimeout(800)
 const hash=()=>a.locator('[data-kind="hombre"] canvas').evaluate(c=>c.toDataURL())
 const green=await hash()
 await a.locator('[data-color="ocean"]').click();assert.notEqual(await hash(),green)
 await a.locator('#accessory').selectOption('scarf');assert.notEqual(await hash(),green)
 await a.locator('[data-color="original"]').click();await a.locator('#accessory').selectOption('none')
 const man=await hash(),woman=await a.locator('[data-kind="mujer"] canvas').evaluate(c=>c.toDataURL())
 assert.notEqual(man,woman)
 await a.screenshot({path:'/tmp/human-lobby.png',fullPage:true})
 await a.locator('#name').fill('Human A');await a.locator('#joinBtn').click();await a.locator('#stage').waitFor({state:'visible'})
 await b.goto(base);await b.locator('[data-kind="mujer"]').click();await b.locator('#name').fill('Human B');await b.locator('#joinBtn').click();await b.locator('#stage').waitFor({state:'visible'})
 await a.emulateMedia({reducedMotion:'no-preference'})
 await a.waitForFunction(()=>[...__po.peers.values()].some(p=>p.name==='Human B'))
 for(const [key,direction]of [['ArrowRight','right'],['ArrowDown','front'],['ArrowLeft','left'],['ArrowUp','back']]){
   await a.evaluate(()=>Object.assign(__po.me,{x:500,y:480,tx:null,ty:null}))
   await a.keyboard.down(key);await a.waitForTimeout(450)
   assert.equal(await a.evaluate(()=>__po.me.direction),direction)
   await a.keyboard.up(key);await a.waitForTimeout(250)
   await b.waitForFunction(d=>[...__po.peers.values()].find(p=>p.name==='Human A')?.direction===d,direction)
   await a.screenshot({path:`/tmp/human-office-${direction}.png`})
 }
 await b.screenshot({path:'/tmp/human-office-remote.png'})
 await a.evaluate(()=>Object.assign(__po.me,{x:190,y:215,tx:null,ty:null}))
 await a.locator('#sitBtn').waitFor({state:'visible'});await a.locator('#sitBtn').click()
 assert(await a.evaluate(()=>__po.me.sitting));await b.waitForFunction(()=>[...__po.peers.values()].some(p=>p.name==='Human A'&&p.sitting))
 await a.screenshot({path:'/tmp/human-office-seated.png'})
 await a.locator('#sitBtn').click();assert.equal(await a.evaluate(()=>__po.me.sitting),false)
 await a.evaluate(()=>Object.assign(__po.me,{x:430,y:1450,tx:null,ty:null}))
 await a.locator('#danceBtn').waitFor({state:'visible'});await a.locator('#danceBtn').click()
 await b.waitForFunction(()=>[...__po.peers.values()].some(p=>p.name==='Human A'&&p.dancing))
 await a.waitForTimeout(300);await a.screenshot({path:'/tmp/human-office-dance.png'})
 await a.emulateMedia({reducedMotion:'reduce'});await a.waitForTimeout(100)
 assert(await a.evaluate(()=>__po.me.dancing))
 await a.locator('#danceBtn').click()
 await a.setViewportSize({width:390,height:844});await a.screenshot({path:'/tmp/human-office-mobile.png'})
 assert(await a.evaluate(()=>document.documentElement.scrollWidth<=innerWidth))
 assert.equal(await a.evaluate(()=>document.querySelector('#selfVideo').readyState>=2),true)
 await a.evaluate(()=>Object.assign(__po.me,{x:120,y:360,tx:null,ty:null}))
 for (const kind of ['orco','lagarto','robot','fantasma']) {
   const guest=await ctx.newPage();guest.on('pageerror',e=>errors.push(e.message))
   await guest.goto(base);await guest.emulateMedia({reducedMotion:'reduce'});await guest.locator(`[data-kind="${kind}"]`).click()
   await guest.waitForTimeout(350)
   await guest.locator('[data-color="original"]').click()
   const original=await guest.locator(`[data-kind="${kind}"] canvas`).evaluate(c=>c.toDataURL())
   await guest.locator('[data-color="ocean"]').click()
   assert((await guest.locator(`[data-kind="${kind}"] canvas`).evaluate(c=>c.toDataURL())) !== original, `${kind}: outfit color must change`)
   await guest.locator('#accessory').selectOption('scarf')
   await guest.emulateMedia({reducedMotion:'no-preference'})
   await guest.locator('#name').fill(kind);await guest.locator('#joinBtn').click();await guest.locator('#stage').waitFor({state:'visible'})
   await a.waitForFunction(k=>[...__po.peers.values()].some(p=>p.name===k&&p.body===k&&p.appearance.color==='ocean'&&p.appearance.accessory==='scarf'),kind)
   for(const [key,direction]of [['ArrowRight','right'],['ArrowDown','front'],['ArrowLeft','left'],['ArrowUp','back']]) {
     await guest.evaluate(()=>Object.assign(__po.me,{x:500,y:480,tx:null,ty:null}))
     await guest.keyboard.down(key);await guest.waitForTimeout(350);await guest.keyboard.up(key)
     assert.equal(await guest.evaluate(()=>__po.me.direction),direction)
     await a.waitForFunction(({k,d})=>[...__po.peers.values()].some(p=>p.name===k&&p.direction===d),{k:kind,d:direction})
   }
   await guest.evaluate(()=>Object.assign(__po.me,{x:190,y:215,tx:null,ty:null}))
   await guest.locator('#sitBtn').waitFor({state:'visible'});await guest.locator('#sitBtn').click()
   await a.waitForFunction(k=>[...__po.peers.values()].some(p=>p.name===k&&p.sitting),kind)
   await guest.screenshot({path:`/tmp/avatar-office-${kind}-seated.png`})
   await guest.locator('#sitBtn').click()
   await guest.evaluate(()=>Object.assign(__po.me,{x:430,y:1450,tx:null,ty:null}))
   await guest.locator('#danceBtn').waitFor({state:'visible'});await guest.locator('#danceBtn').click()
   await a.waitForFunction(k=>[...__po.peers.values()].some(p=>p.name===k&&p.dancing),kind)
   await guest.waitForFunction(()=>__po.me.y-__po.cam.y<__po.VIEW.h-90)
   await guest.screenshot({path:`/tmp/avatar-office-${kind}-dance.png`})
   await guest.close()
 }
 assert.deepEqual(errors,[])
 console.log('PASS: all six characters, color/accessory, live fake camera, four directions, sitting and dancing local and remote, mobile, reduced motion; no browser errors')
 await ctx.close()
}finally{await browser.close()}
